services:
  bloodlink:
    build: .
    ports:
      - "5000:5000"
    environment:
      FLASK_DEBUG: "0"
      PORT: "5000"
      BLOODLINK_DATA_SOURCE: "synthetic"
    volumes:
      - bloodlink_data:/app/data
    restart: unless-stopped

volumes:
  bloodlink_data:
